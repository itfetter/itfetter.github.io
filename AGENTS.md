# AGENTS.md — 项目协作与 AI 修改规范

本文件适用于仓库根目录及其下所有文件。开始修改前先阅读本文件、[README.md](README.md) 与 [PROJECT_HANDOFF.md](PROJECT_HANDOFF.md)，然后核对仓库实际内容；文档与代码不一致时，以代码和 GitHub Pages 当前设置为准，并同步修正文档。

## 1. 项目是什么与业务目标

这是 itfetter 的个人静态博客和展示站，仓库名 `itfetter.github.io`，由 GitHub Pages 发布。目标是用自己的域名呈现个人介绍、文章和项目，并保留指向 CSDN 博客的入口。优先保证阅读体验、易维护、移动端可用和站内内容真实；不把示例文章描述成已正式发表的原创内容。

## 2. 技术栈和项目结构

- HTML5、原生 CSS、原生 JavaScript；无 npm、框架、构建工具、后端及数据库。
- GitHub Pages 从 `main` 分支的 `/ (root)` 发布。
- 当前自定义域名由根目录 `CNAME` 声明为 `itfetter.com`。
- 字体引用 Google Fonts；外部访问 CSDN 和 GitHub。

| 文件 | 作用及修改入口 |
| --- | --- |
| `index.html` | 完整站点。head 管元信息和字体；style 管视觉和响应式；body 管首页、文章列表、关于与阅读容器；底部 script 管文章数据、筛选、搜索、哈希路由。 |
| `CNAME` | Pages 自定义域名，当前内容为 `itfetter.com`，仅允许一个域名。 |
| `README.md` | 对访客和开发者公开的项目介绍、运行与部署说明。 |
| `PROJECT_HANDOFF.md` | 当前进度、变更记录、已知问题和下一步。 |
| `AGENTS.md` | 本文件：协作约束与改动规则。 |

## 3. 核心数据模型与页面行为

目前没有服务端数据模型。文章由 `index.html` 中的 `posts` 数组保存，对象字段为：

| 字段 | 含义 | 约束 |
| --- | --- | --- |
| `id` | 文章哈希标识 | 唯一、稳定，链接格式 `#post/<id>`；改动旧 id 会使旧链接失效。 |
| `title` | 标题 | 用于列表与详情。 |
| `category` | 分类 | 新分类须同步调整页面上的筛选按钮。 |
| `date` | 展示日期 | 当前为展示字符串，保持格式一致。 |
| `summary` | 摘要 | 用于列表与详情。 |
| `html` | 正文 HTML | 当前直接插入 `innerHTML`，仅加入可信且经审阅的内容。 |

`renderList()` 按分类和输入框关键词过滤；`route()` 读取 `location.hash` 展示文章详情。置顶卡片单独写在页面 HTML，当前指向 `#post/hello`，修改置顶文章时必须同步调整标题、摘要、分类和链接。两篇现有文章分别为建站欢迎文和示例随笔。

## 4. 可改与谨慎修改的内容

- 可按需求修改布局、文字、样式、文章、分类、链接和可访问性细节；改动应保持桌面端与手机端一致可用。
- 调整 `posts`、`renderList()`、`route()` 或文章模板时，要验证列表、筛选、搜索、直接打开文章哈希与返回列表。
- `CNAME`、Pages 发布分支及目录、自定义域名和 DNS 涉及线上入口；明确有更改域名的需求且确认 DNS 后才改。不要为了改版删除 `CNAME`。
- 避免无需求时重写整页、引入框架或构建系统、修改既有 `id`、更改 CSDN 地址。确需大改时先说明迁移步骤和旧链接影响。
- 公开仓库及静态页面不得放令牌、密码、私密数据或依赖私密运行时配置。

## 5. API、数据库与前端规则

- **API：** 当前没有自有 API。未来如增加接口，先说明目的、提供者、认证方式、错误处理、速率限制和隐私边界；任何需要密钥的请求放到可信服务端，不能把密钥写入前端。
- **数据库：** 当前没有数据库。未来引入时先写数据模型、迁移和备份方案；不要把浏览器本地存储误写成持久共享数据库。
- **前端：** 保持语义化标签、清晰键盘焦点、图片替代文本和移动端布局；外部新窗口链接加 `rel="noopener noreferrer"`。现有文章正文通过 `innerHTML` 注入，若内容来源从仓库可信常量改为用户输入或外部数据，应先使用安全渲染或严格消毒，避免 XSS。优先使用相对路径和 HTTPS 资源；不要让字体不可用时页面无法阅读。

## 6. 部署、测试及命名规则

- 提交到 `main` 后由 GitHub Pages 从仓库根目录自动部署；部署配置以 **Settings → Pages** 实际状态为准。主地址为 `itfetter.com`，原 `itfetter.github.io` 地址通常会重定向到主地址。
- 如涉及域名/证书，检查 GitHub Pages 中 DNS check、`CNAME`、阿里云 DNS 和 Enforce HTTPS；不能仅凭 HTTP 页面可打开宣称 HTTPS 正常。
- 文档改动检查 Markdown 链接、路径及事实；页面改动至少手动检查首页、文章详情、筛选、搜索、手机宽度和外部链接。无既有自动测试框架，只有引入真正有价值的可执行验证时才添加测试。
- 文件名沿用现有约定：入口 `index.html`、域名 `CNAME`、协作文档大写 Markdown；新增静态资源使用小写英文和连字符命名。JS 字段和函数保持现有 camelCase，文章 `id` 用稳定的简短英文。

## 7. AI 修改代码前后必须遵守的规则

1. 修改前读取目标文件和相关文档，检查当前分支、部署目录、现有链接和工作区改动；不得覆盖他人的未提交修改。
2. 列出改动范围及影响；坚持最小必要修改，不凭空编造功能、文章、统计数据或部署状态。
3. 修改文章时检查 `id` 唯一、分类按钮及置顶卡片是否匹配；修改结构时测试哈希路由的直接访问。
4. 修改后执行与改动相关的检查，并写明实际运行的检查及未验证之处。
5. 更新 `PROJECT_HANDOFF.md` 的进度、变更文件、原因、风险与下一步；涉及使用或部署方式时同步更新 `README.md` 和本文件。
6. 向维护者报告改了什么、为何改、如何验证、还有哪些限制；不要把计划中的功能描述为已完成。

## 8. 开发人员工作规范

- 一个改动解决一个清晰问题；提交信息描述实际变化。
- 变更文章应复核事实、链接、日期和版权；不要把示例内容冒充真实文章。
- 上线前检查 GitHub Pages 部署结果，并在出现异常时通过提交历史回退。
- 网站设计与内容优先服务博客阅读；新增交互应能在无账号、无后端的静态托管环境运行。
